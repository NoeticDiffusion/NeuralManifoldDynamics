# 035 2026-05-20 ds003944 medication rerun

## Research question

Can `ds003944` be re-summarized with the new one-shot cohort-anchor workflow while also carrying medication phenotype metadata from `phenotype/medication.tsv`?

## What changed

- Configured `ds003944` to merge `phenotype/medication.tsv` into participant metadata via `metadata_extraction.datasets.ds003944.participants.extra_tables`.
- Fixed `mnps_9d` runtime resolution so versioned subcoordinate maps replace the legacy/root map instead of being recursively blended with it.
- Added degraded-mode tolerance for `coords_9d` duplicate constant columns, while still rejecting non-constant duplicate trajectories.
- Applied the same tolerant normalization in the cohort-anchor branch.
- Added focused regression tests for:
  - versioned `mnps_9d` config resolution
  - duplicate constant `coords_9d` handling

## Validation

- `python -m pytest tests/test_dataset_subject_runner.py -k "resolve_mnps_9d_runtime_config or subject_runner_exports_reproducibility_provenance"`
- `python -m pytest tests/test_schema.py tests/test_dataset_subject_runner.py -k "duplicate_constant or resolve_mnps_9d_runtime_config"`
- Real-data participant merge smoke check for `ds003944`:
  - `82` participant rows loaded
  - `48 / 82` with non-null `CPZ_at_scan`
- `python -m mndm.cli prerequisite-check --dataset ds003944 --data-dir "M:\datasets\received\openneuro" --config "config/config_ingest_ds003944.yaml"`
  - Passed

## Summarize rerun outcome

- Final summarize run completed successfully:
  - `python -m mndm.cli summarize --dataset ds003944 --data-dir "M:\datasets\received\openneuro" --config "config/config_ingest_ds003944.yaml" --fit-anchor --n-jobs 4`
- Run directory:
  - `E:\Science_Datasets\openneuro\processed\ds003944\neuralmanifolddynamics_ds003944_20260520_115603`
- One-shot anchor written:
  - `E:\Science_Datasets\openneuro\processed\ds003944\neuralmanifolddynamics_ds003944_20260520_115603\anchors\ds003944_all_subjects_iqr_v2_1.json`

## Key result

- The run finished, but produced **zero subject H5 outputs**.
- Manifest summary:
  - `h5 = 0`
  - `summary_json = 0`
  - `qc_summary_json = 0`
  - `qc_reliability_json = 0`
- Immediate cause:
  - `ds003944` feature tables collapse under the current 9D EEG contract after robust normalization.
  - Example (`sub-1448`):
    - `m_a`, `m_e`, and `d_n` become duplicate constant zero trajectories
    - `d_l`, `e_e`, `e_s`, and `e_m` become all-non-finite
  - The pipeline now records this as degraded mode instead of crashing, but the subsequent direct-axis coverage mask removes all epochs, so each subject is skipped.

## Interpretation

- Internal validated result:
  - medication phenotype merge works for `ds003944` at the participant-table level
  - the previous summarize crash was caused by a real `mnps_9d` runtime-config bug plus a strict duplicate-constant guard
- Internal validated result:
  - even after those fixes, `ds003944` still does not yield usable summarize outputs under the current `mnps_projection.normalize=robust_z` + current 9D EEG contract
- Plausible next step:
  - retune `ds003944` specifically, likely by revisiting either the 9D EEG construct choice or the direct-axis coverage policy for this dataset
