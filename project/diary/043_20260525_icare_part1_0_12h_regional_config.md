## Session: I-CARE part1 0-12h regional config

Date: 2026-05-25

### Question
Create a dedicated YAML for I-CARE part1 that exports regional MNPS (3D/9D) and is restricted to the first 12 hours.

### Config added
- New file:
  - `mndm/config/config_ingest_physionet_i-care_2_1_part1_0_12h_regional.yaml`
- Design:
  - Imports base I-CARE config.
  - Uses part1 input folder (`i-care_2_1_random100_longitudinal/training`) generated by downloader config with `max_eeg_hours_per_patient: 12`.
  - Reuses existing part1 features from:
    - `E:/Science_Datasets/physionet/processed/physionet_icare_2_1/features.csv`
  - Writes summarize outputs under:
    - `E:/Science_Datasets/physionet/processed/physionet_icare_2_1/neuralmanifolddynamics_physionet_icare_2_1_<timestamp>`
  - Enables:
    - `regional_mnps.enabled: true`
    - `regional_mnps.stratified.enabled: true`
    - `mnps_9d.enabled: true`
  - Keeps block-Jacobian CSV exports disabled for a leaner first pass.

### Validation
- YAML parsed successfully with `yaml.safe_load`.
- Existing features table contains regional ensemble columns:
  - 84 `__g_*` columns (e.g., `eeg_delta__g_frontal`, `...__g_central`, `...__g_parietal_occipital`).

### Run launched
- Command:
  - `python -m mndm.cli summarize --dataset physionet_icare_2_1 --config config/config_ingest_physionet_i-care_2_1_part1_0_12h_regional.yaml --n-jobs 16`
- Environment:
  - `PYTHONPATH=H:/SourceRepo2/NeuralManifoldDynamics/mndm/src;H:/SourceRepo2/NeuralManifoldDynamics/core/src`
  - `PYTHONIOENCODING=utf-8`
- Early log:
  - `Dropped 90014/2936906 epochs by qc_ok_eeg (policy=eeg_only)`

### Completion status
- Run completed successfully (`exit_code: 0`).
- Output run:
  - `E:/Science_Datasets/physionet/processed/physionet_icare_2_1/neuralmanifolddynamics_physionet_icare_2_1_20260525_141608`
- H5 summary:
  - `h5_total`: 3396
  - `h5_with_regional_mnps`: 3396
  - `h5_with_regional_stratified_any`: 3396
  - `h5_with_jacobian`: 3396
- Run diagnostics:
  - `run_manifest.json` exists (`regional_outputs=true`, `mnps9d=true`)
  - `run_errors.json` exists (`errors_total=88`, `groupings_total=3501`)
  - Error profile:
    - `RuntimeError`: 83
    - `ValueError`: 5
    - Predominant cause: duplicate `coords_9d` columns during stratified normalization (e.g. `m_e -> m_a`).
