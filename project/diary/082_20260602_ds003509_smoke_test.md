# Session Diary 082 - ds003509 smoke test

Date: 2026-06-02

## Goal

Run a focused end-to-end smoke test for `ds003509` after the trigger-enriched
staging update.

## Command

Executed from the repo root with explicit `PYTHONPATH`:

```powershell
python -m mndm.cli prerequisite-check --dataset ds003509 --config "H:/SourceRepo2/NeuralManifoldDynamics/mndm/config/config_ingest_ds003509.yaml"
python -m mndm.cli features --dataset ds003509 --config "H:/SourceRepo2/NeuralManifoldDynamics/mndm/config/config_ingest_ds003509.yaml" --subject 002 --n-jobs 1
python -m mndm.cli summarize --dataset ds003509 --config "H:/SourceRepo2/NeuralManifoldDynamics/mndm/config/config_ingest_ds003509.yaml" --subject 002 --n-jobs 1
```

Smoke target:

- subject `sub-002`
- sessions `ses-01` (`ON`) and `ses-02` (`OFF`)

## Result

Smoke test completed successfully.

- final shell exit code: `0`
- prerequisite check: `overall_ok: True`
- file index written to:
  - `M:/datasets/processed/openneuro/ds003509/file_index.csv`
- features tables written to:
  - `M:/datasets/processed/openneuro/ds003509/features.csv`
  - `M:/datasets/processed/openneuro/ds003509/features.parquet`
- summarize run written to:
  - `M:/datasets/processed/openneuro/ds003509/neuralmanifolddynamics_ds003509_20260602_165832`

Subject/session outputs created:

- `sub-002_ON_simon_conflict/sub-002_ON_simon_conflict.h5`
- `sub-002_OFF_simon_conflict/sub-002_OFF_simon_conflict.h5`
- matching `summary.json`, `qc_summary.json`, `qc_reliability.json`

## Feature-stage observations

Observed from the feature run:

- indexed `84` EEG files from the trigger-enriched staged root
- `sub-002` filter resolved to `2` files
- `ses-01`: `471` EEG epochs
- `ses-02`: `451` EEG epochs
- preprocessing included:
  - early resample `500 -> 250 Hz`
  - average reference
  - EOG regression using `VEOG`
  - pre-resample prune from `67 -> 64` channels

## Event-surface validation

The main smoke-test question was whether the staged compact trigger labels would
survive into summarize outputs.

Confirmed from:

- `stage_mapping_qc.json`
- `sub-002_ON_simon_conflict/summary.json`
- `run_manifest.json`

Key evidence:

- dataset received root:
  - `M:/datasets/received/openneuro/ds003509_trigger_enriched`
- `source_event_column = "event_subtype_role"`
- event labels present in summary/H5 include:
  - `raw_test_stimulus_direct_conflict_onset_sec`
  - `raw_test_stimulus_easy_onset_sec`
  - `raw_test_stimulus_relative_conflict_onset_sec`
  - `raw_test_response_correct_onset_sec`
  - `raw_test_response_incorrect_onset_sec`
  - `raw_training_feedback_reward_onset_sec`
  - `raw_training_feedback_punishment_onset_sec`
  - `raw_training_response_correct_onset_sec`
  - `raw_training_response_incorrect_onset_sec`
  - `raw_training_stimulus_congruent_onset_sec`
  - `raw_training_stimulus_incongruent_onset_sec`

This validates the intended behavior:

- enriched trigger families are exposed as auditable raw event labels
- they are not forced into canonical stage codes
- `stage_frac_labeled` stays `0.0`
- `window_stage_counts` remain at `-1` only

## Notable warnings / residuals

- `cohort_anchored` export was requested by config but skipped because no
  external anchor was active
- the summarize logs emitted several "9D MNPS falsified" conditioning warnings
  for the subject sessions; these did not stop the smoke test
- a `raw_nan_onset_sec` label appears in the summary event list, likely from the
  boundary/status row carrying an empty staged subtype field

## Current interpretation

- Internal validated result:
  - `ds003509` now runs end-to-end with the trigger-enriched staged root
  - summarize emits the intended compact event-family labels
  - the conservative "event provenance only, not canonical stage" design works
