# 028 - 2026-05-16 - Targeted top-up cohort planning

## Session goal

Set up a practical "extra download" strategy that targets missing outcome/CPC balance after the current I-CARE run, without waiting for manual patient-by-patient lookup.

## What was added

- `physionet_ingest/script/download_physionet.py`
  - New subset strategy: `explicit_patient_ids`
  - New knob: `subset.patient_ids`
  - Explicit-ID selection now supports tokens like `284` / `sub-0284` and resolves to canonical IDs in pool.
  - Run summary now stores explicit-patient request count/preview when used.

- `physionet_ingest/script/plan_icare_targeted_topup.py` (new)
  - Scans current local downloaded patient metadata (`CPC`, `Outcome`).
  - Scans missing patients in I-CARE pool and fetches remote sidecar metadata (`training/<id>/<id>.txt`).
  - Greedy target-based selector for extra cohort.
  - Emits:
    - explicit-patient download config
    - JSON planning report with current/projected counts and selected IDs

- `physionet_ingest/config/config_i-care_2_1_longitudinal_targeted_topup.yml` (generated by planner)
  - Strategy: `explicit_patient_ids`
  - 105 selected top-up patient IDs
  - Same 0-12h EEG window cap and same output subdir as current longitudinal run

- `physionet_ingest/README.md`
  - Documented `explicit_patient_ids` + `patient_ids`
  - Added planner command example

- Tests updated in `physionet_ingest/tests/test_download_physionet.py`
  - Explicit strategy selection + missing-ID failure
  - Existing time-window tests retained

## Planner run result

Using targets `CPC1 >= 60`, `CPC5 >= 66`:

- Current local counts: CPC1=28, CPC5=52 (94 patients with metadata)
- Planned top-up size: 105 extra patients
- Projected counts after top-up: CPC1=60, CPC5=66
- Plan report path:
  - `E:/Science_Datasets/physionet/metadata/i-care_2_1_random100_longitudinal/targeted_topup_plan.json`

## Validation

- `pytest physionet_ingest/tests/test_download_physionet.py` -> 18 passed
- Lints for touched physionet files -> no diagnostics

## Notes

- Current background 0-12h download remained running while planning was prepared.
- Top-up config is ready to run after current download completes.
