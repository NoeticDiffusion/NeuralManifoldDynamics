# Session Diary 124 - ds007526 regional MNPS run

Date: 2026-06-10

## Goal

Produce `ds007526` outputs with EEG regional MNPS enabled.

## Why a fresh run was needed

The existing processed tree under `N:/processed/openneuro/ds007526` did not
contain any grouped EEG feature columns of the form `__g_<group>`.

Quick check before the rerun:

- grouped feature columns in `features.parquet`: `0`
- existing summarize run reported `regional_outputs = False`

Because EEG regional MNPS is built from grouped feature surfaces, a summarize-only
rerun would not have been enough. A fresh feature pass was required.

## Config change

Updated `mndm/config/config_ingest_ds007526.yaml` with:

- `robustness.ensembles.enabled: true`
- ensemble groups:
  - `frontal`
  - `central`
  - `parietal_occipital`
  - `temporal`
- `regional_mnps.enabled: true`
- `regional_mnps.stratified.enabled: true`
- `regional_mnps.block_jacobians.enabled: false`

The first regional pass intentionally kept block Jacobians off to focus on the
core regional trajectory outputs.

## Fresh run

Ran to a new processed root to avoid reusing the old non-regional feature cache:

```powershell
$env:PYTHONPATH="H:/SourceRepo2/NeuralManifoldDynamics/mndm/src;H:/SourceRepo2/NeuralManifoldDynamics/core/src;H:/SourceRepo2/NeuralManifoldDynamics/openneuro_ingest/src;H:/SourceRepo2/NeuralManifoldDynamics/apollo_ingest/src;H:/SourceRepo2/NeuralManifoldDynamics/vitaldb_ingest/src;H:/SourceRepo2/NeuralManifoldDynamics"
python -m mndm.cli all --dataset ds007526 --config "H:/SourceRepo2/NeuralManifoldDynamics/mndm/config/config_ingest_ds007526.yaml" --data-dir "N:/received" --out-dir "N:/processed/openneuro_ds007526_regional" --n-jobs 4
```

Result:

- completed successfully with `exit_code: 0`
- run directory:
  - `N:/processed/openneuro_ds007526_regional/ds007526/neuralmanifolddynamics_ds007526_20260610_105212`

## Regional verification

During summarize, logs repeatedly reported:

- `Regional MNPS for ... : 4 networks computed, 0 dropped`

Regional CSV written:

- `regional_mnps_subjects_105212.csv`

Observed CSV shape:

- `1108` rows total
- expected interpretation: `277 recordings × 4 networks`

CSV columns include:

- `subject_id`
- `session_id`
- `condition_label`
- `task_label`
- `network_label`
- `n_timepoints`
- summary stats over `m`, `d`, `e`
- stratified conditioning diagnostics such as:
  - `strat9_condition_number`
  - `strat9_pc1_var`
  - `strat9_pc2_var`
  - `strat9_pc3_var`
  - `strat9_pc1_3_cumulative_var`
  - `strat9_falsified`

## H5 verification

Inspected:

- `N:/processed/openneuro_ds007526_regional/ds007526/neuralmanifolddynamics_ds007526_20260610_105212/sub-029_rest/sub-029_rest.h5`

Confirmed:

- top-level `/regional_mnps` group exists
- networks present:
  - `central`
  - `frontal`
  - `parietal_occipital`
  - `temporal`
- sample network subgroup (`/regional_mnps/frontal`) contains:
  - `jacobian`
  - `mnps`
  - `mnps_dot`
  - `stratified`
  - `subject_anchored`

## Notes

- The fresh regional run lives under a separate processed root:
  - `N:/processed/openneuro_ds007526_regional`
- This preserves the earlier non-regional / metadata-join runs under
  `N:/processed/openneuro`.
