## Session: ds003509 + ds003506 dual cohort rerun

### Date
2026-06-08

### Goal
Ensure dual cohort export contract is active for both Parkinson datasets (`ds003509`, `ds003506`) and rerun both full pipelines.

### Config verification
No YAML edits were needed in this session.

Both overlays already had the required contract:

- `mnps_projection.export_contracts.subject_anchored: true`
- `mnps_projection.export_contracts.cohort_anchored: true`
- `mnps_projection.anchor_auto_fit.enabled: true`
- dataset-specific `anchor_id` (`ds003509_cohort_auto_v2_1`, `ds003506_cohort_auto_v2_1`)
- `anchor_source`, `scale_method: iqr`, `min_subjects: 3`

### Full reruns
1. `ds003509`
   - Command:
     - `python -m mndm.cli all --dataset ds003509 --config mndm/config/config_ingest_ds003509.yaml --n-jobs 12`
   - Result: exit code 0
   - Run dir:
     - `M:/datasets/processed/openneuro/ds003509/neuralmanifolddynamics_ds003509_20260608_151832`

2. `ds003506`
   - Command:
     - `python -m mndm.cli all --dataset ds003506 --config mndm/config/config_ingest_ds003506.yaml --n-jobs 12`
   - Result: exit code 0
   - Run dir:
     - `M:/datasets/processed/openneuro/ds003506/neuralmanifolddynamics_ds003506_20260608_163629`

### Verification of realized dual-cohort layers
For both run manifests:

- `coordinate_contracts.requested_contracts` includes `subject_anchored` + `cohort_anchored`
- `coordinate_contracts.realized_contracts` includes `subject_anchored` + `cohort_anchored`
- `feature_anchors: true`
- `coords_3d_cohort_anchored: true`
- `coords_9d_cohort_anchored: true`
- counts:
  - `h5_with_feature_anchors: 84`
  - `h5_with_coords_3d_cohort_anchored: 84`
  - `h5_with_coords_9d_cohort_anchored: 84`

`run_manifest.json` also records `anchor_auto_fit.enabled: true` with dataset-specific anchor IDs for both datasets.
